- implement calling convention for matrices mat(dst,src) voted June 5
- do the above through new matrix_operator class, also can be used to combine
  matrices with Dag Inv versions so that we can define adj(mat), inv(mat) !!  Neat!
- A2A, HVP
- Complete sparse/split grid implementation
- sources
- verbose=eval -> Bytes/s & Flops/s for expression evaluation
